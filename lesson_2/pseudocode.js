
//Returns the sum of two numbers
//START
// get input for first number 
//get input for second number
//perform the addition on the two numbers 
//return the answer to the user


//a function that takes an array of strings, and returns a string that is all those strings concatenated together
//START
//function that gets the array of strings
//join all of the strings in the array 
//return the answer to the user.
// END


//a function that takes an array of integers, and returns a new array with every other element
//START
//Make funtion that gets the input array of integers
// SET a new array that will be output
//iterate through the input array
//if the index of the array is even, push it to the new array
//otherwise continue iterating over the array.
//return the new array that contains every other value of the array.
//END